
<h3><%= @outlet_produces.sort_by {|k| k['outlet_id']}.inspect %> </h3>

<%= react_component 'Orders', { data: @orders } %>

<%= react_component 'PlaceOrders', { data: @outlet_produces.sort_by {|k| k['outlet_id']}, current_user: current_user } %>
