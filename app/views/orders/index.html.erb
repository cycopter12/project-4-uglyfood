<h1>All Orders</h1>

<% @orders.in_groups_of(3) do |group| %>
  <div class="row">
    <% group.compact.each do |order| %>
      <div class="col-md-4">
        <p>Cost: <%= order.cost %></p>
        
      </div>
    <% end %>
  </div>
<% end %>
